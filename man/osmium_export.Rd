% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{osmium_export}
\alias{osmium_export}
\title{Export OSM data to GeoJSON, geopackage-friendly text, or PostgreSQL COPY format}
\usage{
osmium_export(
  input,
  output = NULL,
  output_format = NULL,
  config = NULL,
  add_unique_id = NULL,
  format_option = NULL,
  geometry_types = NULL,
  index_type = NULL,
  keep_untagged = FALSE,
  attributes = NULL,
  show_errors = FALSE,
  stop_on_error = FALSE,
  overwrite = FALSE,
  fsync = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{input}{Path to an input OSM file.}

\item{output}{Output file path. If NULL, output goes to stdout and is
returned as a character string.}

\item{output_format}{Output format, e.g. "geojson", "geojsonseq",
"text", "pg" (default depends on `output`'s extension).}

\item{config}{Path to a JSON config file with export rules (tag
include/exclude lists, attribute selection, ...).}

\item{add_unique_id}{Add a unique id to each feature: "counter" or
"type_id".}

\item{format_option}{Character vector of `key=value` output format
options.}

\item{geometry_types}{Comma-separated geometry types to write, e.g.
"point,linestring,polygon" (the default).}

\item{index_type}{Node location index type (default "flex_mem"; see
`osmium export --show-index-types`).}

\item{keep_untagged}{If TRUE, also export features without any tags.}

\item{attributes}{Comma-separated list of OSM attributes to add to
each feature (e.g. "version,timestamp"); none by default.}

\item{show_errors}{If TRUE, print geometry errors to stdout instead of
silently skipping them.}

\item{stop_on_error}{If TRUE, stop at the first geometry error instead
of skipping it.}

\item{fsync, overwrite, verbose}{See [osmium_cat()].}
}
\value{
If `output` is NULL, the written data as a character string.
  Otherwise, the `output` path, invisibly.
}
\description{
Wraps `osmium export`. Converts OSM nodes/ways/(multipolygon)
relations into geometry-bearing features.
}

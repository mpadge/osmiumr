% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{osmium_extract}
\alias{osmium_extract}
\title{Create a geographic extract from an OSM file}
\usage{
osmium_extract(
  input,
  output = NULL,
  bbox = NULL,
  polygon = NULL,
  config = NULL,
  directory = NULL,
  strategy = c("complete_ways", "simple", "smart"),
  option = NULL,
  with_history = FALSE,
  set_bounds = FALSE,
  clean = NULL,
  input_format = NULL,
  overwrite = FALSE,
  fsync = FALSE,
  generator = NULL,
  output_header = NULL,
  verbose = FALSE
)
}
\arguments{
\item{input}{Path to an input OSM file.}

\item{output}{Output file path. Ignored (with a warning from osmium)
if `config` is given, since a config file specifies its own
per-extract outputs.}

\item{bbox}{Numeric vector of length 4: `c(left, bottom, right, top)`
(i.e. min lon, min lat, max lon, max lat).}

\item{polygon}{Path to a polygon file (`.poly` format or a GeoJSON/OSM
file with a single (multi)polygon) describing the extract boundary.}

\item{config}{Path to a JSON config file describing one or more
extracts (see `osmium help extract`); when given, `directory`,
`output`, and `output_format` come from the config file instead.}

\item{directory}{Output directory for extracts defined via `config`.}

\item{strategy}{Extract strategy: "complete_ways" (default, keeps
ways with any node in the area, adding missing nodes), "simple"
(cuts ways at the boundary), or "smart" (like complete_ways but
also completes multipolygon relations).}

\item{option}{Character vector of `key=value` strategy options (`-S`).}

\item{with_history}{If TRUE, input and output are OSM history files.}

\item{set_bounds}{If TRUE, set the extract's bounding box in the
output file header.}

\item{clean}{Character vector of attributes to strip: any of
"version", "changeset", "timestamp", "uid", "user".}

\item{input_format, overwrite, fsync, generator, output_header, verbose}{See [osmium_cat()].}
}
\value{
The `output` path (or `directory`, if `config` was used),
  invisibly.
}
\description{
Wraps `osmium extract`. Exactly one of `bbox`, `polygon`, or `config`
must be given.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tags_filter.R
\name{osmium_tags_filter}
\alias{osmium_tags_filter}
\title{Filter OSM data based on tag expressions}
\usage{
osmium_tags_filter(
  input,
  output,
  expressions = NULL,
  expressions_file = NULL,
  invert_match = FALSE,
  omit_referenced = FALSE,
  remove_tags = FALSE,
  object_type = NULL,
  input_format = NULL,
  overwrite = FALSE,
  fsync = FALSE,
  generator = NULL,
  output_header = NULL,
  verbose = FALSE
)
}
\arguments{
\item{input}{Path to an input OSM file.}

\item{output}{Output file path.}

\item{expressions}{Character vector of filter expressions (see
`osmium help tags-filter` for expression syntax, e.g.
`"n/amenity=cafe"` or `"w/highway"`). Ignored if `expressions_file`
is given.}

\item{expressions_file}{Path to a file with one filter expression per
line, used instead of `expressions`.}

\item{invert_match}{If TRUE, keep objects that do NOT match.}

\item{omit_referenced}{If TRUE, don't automatically pull in objects
referenced by matching objects (e.g. a way's nodes).}

\item{remove_tags}{If TRUE, strip tags from objects kept only because
they're referenced by a matching object.}

\item{object_type}{Character vector restricting output to given
object types: any of "node", "way", "relation".}

\item{input_format, overwrite, fsync, generator, output_header, verbose}{See [osmium_cat()].}
}
\value{
The `output` path, invisibly.
}
\description{
Wraps `osmium tags-filter`. Keeps objects matching one or more filter
expressions (plus, by default, any objects they reference), optionally
inverting the match or stripping tags from non-matching objects.
}

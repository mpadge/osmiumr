% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fileinfo.R
\name{osmium_fileinfo}
\alias{osmium_fileinfo}
\title{Show information about an OSM file}
\usage{
osmium_fileinfo(
  input,
  extended = FALSE,
  crc = FALSE,
  object_type = NULL,
  get = NULL,
  verbose = FALSE
)
}
\arguments{
\item{input}{Path to an input OSM file.}

\item{extended}{If TRUE, also scan the whole file to compute extended
statistics (bounding box, counts, CRC, ...), not just the header.}

\item{crc}{If TRUE, calculate a CRC32 checksum (implies `extended`).}

\item{object_type}{Character vector of object types to include when
`extended = TRUE`: any of "node", "way", "relation", "changeset".}

\item{get}{A single dotted variable name (see `osmium help fileinfo` /
`--show-variables`) to retrieve instead of the full report; returned
as a length-1 character string.}

\item{verbose}{If TRUE, print verbose progress information.}
}
\value{
If `get` is NULL, a nested list parsed from osmium's `--json`
  output (via jsonlite). If `get` is supplied, a length-1 character
  vector.
}
\description{
Wraps `osmium fileinfo`.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_refs.R
\name{osmium_check_refs}
\alias{osmium_check_refs}
\title{Check referential integrity of an OSM file}
\usage{
osmium_check_refs(
  input,
  show_ids = FALSE,
  check_relations = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{input}{Path to an input OSM file.}

\item{show_ids}{If TRUE, include the IDs of missing objects in the
returned result (parsed from stdout).}

\item{check_relations}{If TRUE, also check relation members (not just
way nodes).}

\item{verbose}{If TRUE, print verbose progress information.}
}
\value{
A list with element `ok` (TRUE if no references are missing)
  and `missing_ids` (character vector of lines like "n123 in w456",
  populated only if `show_ids = TRUE`; empty otherwise).
}
\description{
Wraps `osmium check-refs`. Checks that every node referenced by a way
(and, optionally, every node/way/relation referenced by a relation) is
actually present in the file.
}
